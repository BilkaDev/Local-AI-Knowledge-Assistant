services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      STREAMLIT_SERVER_HEADLESS: "true"
    ports:
      - "${APP_PORT:-8501}:8501"
    volumes:
      - .:/workspace
    restart: unless-stopped

Why must chunk_overlap be smaller than chunk_size?	Dlaczego chunk_overlap musi być mniejszy niż chunk_size?	If overlap is equal to or larger than chunk size, the step becomes zero or negative and chunking can loop incorrectly. This rule keeps the algorithm moving forward predictably.	Jeśli overlap jest równy lub większy od chunk size, krok staje się zerowy albo ujemny i chunking może działać błędnie lub zapętlać się. Ta zasada gwarantuje przewidywalny ruch do przodu.
How do you quickly decide if an ingestion run was successful?	Jak szybko ocenić, czy run ingestion był udany?	Check files_ingested, chunks_created, and failures together. A clean run has ingested files, created chunks, and no failures.	Sprawdź razem files_ingested, chunks_created i failures. Czysty run ma przetworzone pliki, utworzone chunki i brak failures.
What is the practical difference between a fatal error and a per-file failure?	Jaka jest praktyczna różnica między fatal error a błędem pojedynczego pliku?	A fatal error stops the entire run and returns a non-zero exit code. A per-file failure is logged, while other files continue processing.	Fatal error zatrzymuje cały run i zwraca niezerowy kod wyjścia. Błąd pojedynczego pliku trafia do raportu, a pozostałe pliki są dalej przetwarzane.
When failures are present but some files are ingested, what should you do next?	Co zrobić, gdy są failures, ale część plików została zaingestowana?	Treat it as a partial success and inspect failure reasons first. Fix only the problematic files, then rerun ingestion.	Traktuj to jako częściowy sukces i najpierw sprawdź powody błędów. Napraw tylko problematyczne pliki i uruchom ingestion ponownie.
Why is README plus .env configuration important for team work?	Dlaczego README i konfiguracja .env są ważne w pracy zespołowej?	They make the run procedure and defaults explicit, so everyone executes ingestion the same way. This reduces hidden differences and debugging time.	Ujawniają procedurę uruchamiania i domyślne parametry, więc każdy odpala ingestion tak samo. To zmniejsza ukryte różnice i czas debugowania.
How does chunk overlap affect retrieval quality trade-offs?	Jak overlap wpływa na kompromis jakości retrieval?	More overlap preserves context continuity across chunk boundaries, which can improve recall. But it also increases chunk count and storage/computation cost.	Większy overlap lepiej zachowuje ciągłość kontekstu na granicach chunków, co może poprawić recall. Zwiększa jednak liczbę chunków oraz koszt przechowywania i obliczeń.
What signal suggests chunking parameters might be poorly chosen?	Jaki sygnał sugeruje źle dobrane parametry chunkingu?	If chunks_created is unexpectedly high or very low relative to data size, parameters may be off. Frequent empty/no-chunk failures are another warning sign.	Jeśli chunks_created jest podejrzanie wysokie lub bardzo niskie względem danych, parametry mogą być źle dobrane. Częste błędy typu empty/no-chunk to kolejny sygnał ostrzegawczy.
Why normalize text before chunking?	Dlaczego normalizować tekst przed chunkingiem?	Normalization makes chunking behavior more stable by removing noisy whitespace differences. It improves consistency across files and easier debugging.	Normalizacja stabilizuje działanie chunkingu, usuwając szum w postaci nieregularnych spacji i nowych linii. Daje bardziej spójne wyniki między plikami i ułatwia debugowanie.
What should the minimum ingestion report contain for operations?	Co minimalnie powinien zawierać raport ingestion dla operacyjności?	It should include scanned files, ingested files, chunks created, and failure details. This is enough to assess health and decide next actions quickly.	Powinien zawierać liczbę znalezionych plików, przetworzonych plików, utworzonych chunków oraz szczegóły błędów. To wystarczy, by szybko ocenić stan i podjąć następne kroki.
How does Docker-first execution reduce environment problems?	Jak podejście Docker-first zmniejsza problemy środowiskowe?	It standardizes runtime dependencies and paths across machines. This makes results more reproducible for the whole team.	Standaryzuje zależności i ścieżki uruchomieniowe między maszynami. Dzięki temu wyniki są bardziej powtarzalne dla całego zespołu.
Why was a hybrid Day 3 approach (loader-first + configurable chunking) a good decision?	Dlaczego hybrydowa decyzja Day 3 (loader-first + konfigurowalny chunking) była dobra?	It delivered reliable ingestion quickly while preserving tuning flexibility for retrieval quality. The team reduced delivery risk without blocking future improvements.	Pozwoliła szybko dowieźć stabilny ingestion i jednocześnie zachować możliwość strojenia jakości retrieval. Zespół zmniejszył ryzyko dowozu bez blokowania dalszych ulepszeń.
What does a high files_scanned but low files_ingested ratio usually indicate?	Co zwykle oznacza wysoki files_scanned i niski files_ingested?	Many discovered files are failing validation or parsing. You should inspect failure reasons and data quality before changing algorithm logic.	Wiele znalezionych plików nie przechodzi walidacji lub parsowania. Najpierw sprawdź powody błędów i jakość danych, zanim zmienisz logikę algorytmu.
Why keep failure handling per file instead of failing fast for all data?	Dlaczego obsługiwać błędy per plik zamiast zatrzymywać cały proces?	Per-file handling improves resilience and keeps useful output available even with bad inputs. It supports iterative cleanup rather than all-or-nothing runs.	Obsługa per plik zwiększa odporność i pozwala zachować użyteczny wynik mimo wadliwych danych. Wspiera iteracyjne czyszczenie zamiast podejścia wszystko-albo-nic.
How can you tell that ingestion output is ready for the embedding stage?	Jak rozpoznać, że wynik ingestion jest gotowy do etapu embeddingów?	You need non-zero chunks_created and acceptable failure levels for your quality bar. Chunk metadata should clearly link chunks back to source files.	Potrzebujesz niezerowego chunks_created i akceptowalnego poziomu błędów względem Twojego progu jakości. Metadane chunków powinny jasno wskazywać pliki źródłowe.
What is a common mistake when interpreting ingestion metrics?	Jaki jest częsty błąd przy interpretacji metryk ingestion?	Looking only at files_ingested and ignoring failures can hide serious data issues. Metrics must be read together as a system signal.	Patrzenie tylko na files_ingested i ignorowanie failures może ukryć poważne problemy z danymi. Metryki trzeba czytać łącznie jako jeden sygnał systemowy.
When should you tune chunk_size and chunk_overlap?	Kiedy warto stroić chunk_size i chunk_overlap?	Tune them when retrieval quality is weak or chunk counts are operationally costly. Make small changes and compare report trends between runs.	Strojenie ma sens, gdy jakość retrieval jest słaba albo liczba chunków jest kosztowna operacyjnie. Wprowadzaj małe zmiany i porównuj trendy raportów między runami.
What operational habit improves ingestion reliability over time?	Jaki nawyk operacyjny poprawia niezawodność ingestion w czasie?	Run ingestion with consistent parameters and keep reports for comparison. Trend visibility helps detect regressions early.	Uruchamiaj ingestion ze stałymi parametrami i zachowuj raporty do porównań. Widoczność trendów pomaga szybko wykrywać regresje.
Why is concept focus more important than syntax details at this stage?	Dlaczego na tym etapie ważniejsze są koncepty niż detale składni?	Strong conceptual understanding helps you debug and make better decisions under change. Syntax can be looked up quickly, but reasoning cannot be replaced.	Mocne rozumienie konceptów pomaga debugować i podejmować lepsze decyzje przy zmianach. Składnię można szybko sprawdzić, ale myślenia systemowego nie da się zastąpić.
What is ingestion in a RAG workflow?	Czym jest ingestion w przepływie RAG?	Ingestion is the stage where source files are loaded, cleaned, and prepared for retrieval. It transforms raw documents into structured chunks plus metadata.	Ingestion to etap, w którym pliki źródłowe są wczytywane, czyszczone i przygotowywane do retrieval. Zamienia surowe dokumenty w uporządkowane chunki z metadanymi.
What is a chunk and why do we use it?	Czym jest chunk i po co go używamy?	A chunk is a smaller piece of a larger document used as a retrieval unit. Smaller units improve matching precision and make context selection easier.	Chunk to mniejszy fragment większego dokumentu używany jako jednostka wyszukiwania. Mniejsze jednostki poprawiają precyzję dopasowania i ułatwiają dobór kontekstu.
What does chunk_size control?	Co kontroluje chunk_size?	Chunk size controls how much text can be stored in one chunk. It directly affects context granularity and the number of chunks created.	Chunk size kontroluje, ile tekstu może trafić do jednego chunka. Bezpośrednio wpływa na granularność kontekstu i liczbę tworzonych chunków.
What does chunk_overlap control?	Co kontroluje chunk_overlap?	Chunk overlap controls how much text is shared between neighboring chunks. It helps preserve meaning near chunk boundaries.	Chunk overlap kontroluje, ile tekstu jest współdzielone między sąsiednimi chunkami. Pomaga zachować sens na granicach chunków.
What is retrieval in RAG?	Czym jest retrieval w RAG?	Retrieval is the step that finds the most relevant chunks for a user query. The model then answers using those selected chunks as context.	Retrieval to krok, który znajduje najbardziej pasujące chunki do pytania użytkownika. Model odpowiada potem, używając tych chunków jako kontekstu.
What is an embedding in simple terms?	Czym jest embedding w prostych słowach?	An embedding is a vector representation of text that captures semantic similarity. Similar meanings end up close to each other in vector space.	Embedding to wektorowa reprezentacja tekstu, która oddaje podobieństwo znaczeniowe. Teksty o podobnym sensie są blisko siebie w przestrzeni wektorowej.
What is a vector database used for?	Do czego służy baza wektorowa?	A vector database stores embeddings and enables fast similarity search. In RAG, it helps retrieve relevant chunks efficiently.	Baza wektorowa przechowuje embeddingi i umożliwia szybkie wyszukiwanie podobieństwa. W RAG pomaga sprawnie znajdować trafne chunki.
What does metadata mean for chunks?	Co oznaczają metadane chunków?	Metadata is extra information about each chunk, such as source file and character range. It improves traceability and debugging.	Metadane to dodatkowe informacje o chunku, np. plik źródłowy i zakres znaków. Zwiększają możliwość śledzenia pochodzenia i ułatwiają debugowanie.
What does top_k mean during retrieval?	Co oznacza top_k podczas retrieval?	top_k is the number of best-matching chunks returned for a query. Higher values give more context but can add noise.	top_k to liczba najlepiej dopasowanych chunków zwracanych dla zapytania. Wyższa wartość daje więcej kontekstu, ale może dodać szum.
What is normalization in this pipeline?	Czym jest normalizacja w tym pipeline?	Normalization is lightweight cleanup, such as collapsing repeated whitespace. Its goal is consistency, not changing document meaning.	Normalizacja to lekkie czyszczenie, np. redukcja powtarzających się spacji i nowych linii. Jej celem jest spójność, a nie zmiana znaczenia dokumentu.
What is a CLI in this project context?	Czym jest CLI w tym projekcie?	CLI is a command-line interface to run ingestion with clear parameters. It makes runs repeatable across teammates and environments.	CLI to interfejs wiersza poleceń do uruchamiania ingestion z jasnymi parametrami. Dzięki temu uruchomienia są powtarzalne między osobami i środowiskami.
What is a report artifact and why keep it?	Czym jest artifact raportu i po co go trzymać?	A report artifact is a saved JSON summary of one ingestion run. Keeping it lets you compare runs and detect regressions early.	Artifact raportu to zapisane podsumowanie JSON z jednego runa ingestion. Przechowywanie pozwala porównywać runy i wcześnie wykrywać regresje.
What does Docker-first mean operationally?	Co operacyjnie oznacza Docker-first?	It means running ingestion in a container with controlled dependencies and paths. This reduces "works on my machine" issues.	Oznacza uruchamianie ingestion w kontenerze ze stałymi zależnościami i ścieżkami. To ogranicza problemy typu "u mnie działa".
What is the role of .env in this workflow?	Jaka jest rola .env w tym workflow?	The .env file stores default runtime settings such as chunk size and report path. It keeps configuration explicit and easy to change without code edits.	Plik .env przechowuje domyślne ustawienia uruchomieniowe, np. chunk size i ścieżkę raportu. Dzięki temu konfiguracja jest jawna i łatwa do zmiany bez edycji kodu.
What is a pipeline in practical terms?	Czym jest pipeline w praktyce?	A pipeline is an ordered chain of processing steps from input to output. If one stage is unstable, downstream quality also drops.	Pipeline to uporządkowany łańcuch kroków przetwarzania od wejścia do wyjścia. Jeśli jeden etap jest niestabilny, jakość kolejnych etapów też spada.
What is ingestion?	Czym jest ingestion?	Ingestion is the stage where documents are loaded, cleaned, and split for later retrieval. It prepares raw files for downstream RAG processing.	Ingestion to etap, w którym dokumenty są wczytywane, oczyszczane i dzielone pod późniejszy retrieval. Przygotowuje surowe pliki do dalszego przetwarzania w RAG.
What is a chunk?	Czym jest chunk?	A chunk is a small piece of text extracted from a larger document. It is the basic unit used during retrieval.	Chunk to mały fragment tekstu wycięty z większego dokumentu. Jest podstawową jednostką używaną podczas retrieval.
What is chunking?	Czym jest chunking?	Chunking is the process of splitting long text into smaller pieces. It improves matching and context handling in RAG.	Chunking to proces dzielenia długiego tekstu na mniejsze fragmenty. Poprawia dopasowanie i obsługę kontekstu w RAG.
What does chunk_size mean?	Co oznacza chunk_size?	chunk_size is the maximum text length allowed per chunk. In this project it is measured in characters.	chunk_size to maksymalna długość tekstu w jednym chunku. W tym projekcie jest liczona w znakach.
What does chunk_overlap mean?	Co oznacza chunk_overlap?	chunk_overlap is the number of characters repeated between consecutive chunks. It helps preserve context across boundaries.	chunk_overlap to liczba znaków powtarzanych między kolejnymi chunkami. Pomaga zachować kontekst na granicach fragmentów.
What does normalize text mean?	Co oznacza normalize text (normalizacja)?	Text normalization simplifies formatting noise, for example by collapsing extra whitespace. It keeps processing more consistent across files.	Normalizacja tekstu upraszcza szum formatowania, na przykład redukując nadmiarowe spacje. Dzięki temu przetwarzanie jest bardziej spójne między plikami.
What is a loader?	Czym jest loader?	A loader is the part of code that reads source files into memory. Here it handles formats like TXT and PDF.	Loader to część kodu, która wczytuje pliki źródłowe do pamięci. Tutaj obsługuje formaty takie jak TXT i PDF.
What are supported extensions?	Czym są supported extensions?	Supported extensions are file types accepted by the ingestion pipeline. Files outside this list are skipped or rejected.	Supported extensions to typy plików akceptowane przez pipeline ingestion. Pliki spoza tej listy są pomijane lub odrzucane.
What does files_scanned mean?	Co oznacza files_scanned?	files_scanned is the number of input files discovered for processing. It shows how much data was found in the target directory.	files_scanned to liczba plików wejściowych wykrytych do przetworzenia. Pokazuje, ile danych znaleziono w docelowym katalogu.
What does files_ingested mean?	Co oznacza files_ingested?	files_ingested is the number of files successfully processed into chunks. It excludes files that failed during ingestion.	files_ingested to liczba plików, które zostały poprawnie przetworzone na chunki. Nie obejmuje plików, które zakończyły się błędem.
What does chunks_created mean?	Co oznacza chunks_created?	chunks_created is the total number of chunks produced in a run. It helps estimate retrieval coverage and storage cost.	chunks_created to łączna liczba chunków utworzonych w danym runie. Pomaga ocenić pokrycie retrieval i koszt przechowywania.
What is a failure in ingestion?	Czym jest failure w ingestion?	A failure is an error tied to a specific file during processing. It is logged so the run can continue with other files.	Failure to błąd przypisany do konkretnego pliku podczas przetwarzania. Jest raportowany, aby run mógł iść dalej dla pozostałych plików.
What is a fatal error?	Czym jest fatal error?	A fatal error is a critical issue that stops the entire ingestion run. It usually returns a non-zero exit code.	Fatal error to krytyczny problem, który zatrzymuje cały run ingestion. Zwykle kończy się niezerowym kodem wyjścia.
What is an ingestion report?	Czym jest raport ingestion?	An ingestion report is a JSON summary of run metrics and failure details. It provides quick operational visibility and troubleshooting context.	Raport ingestion to podsumowanie JSON z metrykami runa i szczegółami błędów. Daje szybki wgląd operacyjny i kontekst do diagnozy.
What is CLI in this context?	Czym jest CLI w tym kontekście?	CLI is running the ingestion script via terminal commands with explicit parameters. It makes execution repeatable and scriptable.	CLI to uruchamianie skryptu ingestion komendami w terminalu z jawnymi parametrami. To zwiększa powtarzalność i automatyzowalność uruchomień.
What is argparse used for?	Do czego służy argparse?	argparse is a Python library for parsing command-line arguments. It maps flags like --data-dir into usable program settings.	argparse to biblioteka Pythona do obsługi argumentów wiersza poleceń. Zamienia flagi typu --data-dir na ustawienia używane przez program.
What is .env used for?	Do czego służy .env?	The .env file stores default configuration values used at runtime. It separates configuration from source code.	Plik .env przechowuje domyślne wartości konfiguracji używane podczas uruchomienia. Oddziela konfigurację od kodu źródłowego.
What is Docker used for here?	Do czego służy Docker tutaj?	Docker runs the app in an isolated, reproducible environment. It reduces dependency and path differences across machines.	Docker uruchamia aplikację w odizolowanym i powtarzalnym środowisku. Ogranicza różnice zależności i ścieżek między maszynami.
What is RAG?	Czym jest RAG?	RAG stands for Retrieval-Augmented Generation, where a model answers using retrieved context. This improves factual grounding compared to generation alone.	RAG oznacza Retrieval-Augmented Generation, czyli model odpowiada z użyciem wyszukanego kontekstu. To poprawia oparcie odpowiedzi o fakty względem samej generacji.
What is retrieval?	Czym jest retrieval?	Retrieval is the step of finding the most relevant chunks for a user question. Those chunks are passed to the model as context.	Retrieval to etap wyszukiwania najbardziej trafnych chunków do pytania użytkownika. Te chunki są przekazywane modelowi jako kontekst.
What is an embedding?	Czym jest embedding?	An embedding is a numeric vector representation of text used to measure semantic similarity. Similar texts have embeddings that are close in vector space.	Embedding to numeryczna reprezentacja tekstu w postaci wektora, używana do mierzenia podobieństwa znaczeniowego. Podobne teksty mają embeddingi blisko siebie w przestrzeni wektorowej.
What is a vector DB?	Czym jest vector DB?	A vector database stores embeddings and supports fast similarity search. It is a core component for scalable retrieval in RAG systems.	Baza wektorowa przechowuje embeddingi i wspiera szybkie wyszukiwanie podobieństwa. To kluczowy komponent skalowalnego retrieval w systemach RAG.
What does top_k mean?	Co oznacza top_k?	top_k is the number of top retrieval results returned for each query. It controls how much retrieved context is sent to the model.	top_k to liczba najlepszych wyników retrieval zwracanych dla każdego zapytania. Kontroluje, ile wyszukanego kontekstu trafia do modelu.
What is metadata in chunk processing?	Czym są metadane w przetwarzaniu chunków?	Metadata is additional information about a chunk, such as source file and character span. It improves traceability, filtering, and debugging.	Metadane to dodatkowe informacje o chunku, na przykład plik źródłowy i zakres znaków. Zwiększają śledzalność, filtrowanie i wygodę debugowania.
